# Deprecated MCP Path

This folder contains the retired Ghost server-based integration.

It is archived for reference only. It is not supported, not tested as a
production path, and should not be used for long-running browser sessions.

The supported path is the CLI in the repo root:

- `./ghost-cli call ...`
- `./ghost-cli repl`
